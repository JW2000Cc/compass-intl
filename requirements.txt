flask>=3.0
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.5
python-dotenv>=1.0
anthropic>=0.30
openai>=1.30
markitdown>=0.0.1
python-docx>=1.1
pypdf>=4.0
httpx>=0.27
jinja2>=3.1
python-jobspy>=1.1.80
markupsafe>=2.1
# Multi-engine search wrapper used by contact_finder (DDG/Bing/Brave/Yahoo unified)
ddgs>=9.14
# HTML parsing fallback for contact_finder
beautifulsoup4>=4.12
# Optional: only needed if you use Compass via Claude Code Skill (MCP path)
# Comment out to skip — Compass core works without it.
mcp>=0.9; python_version >= "3.10"
# Optional: Gmail integration for auto-detecting application status
google-api-python-client>=2.100
google-auth-oauthlib>=1.2

# Template inspiration — DOCX/HTML/PDF/image template borrow
docxtpl>=0.18           # DOCX template with {{ }} placeholders, format-preserving
pdf2docx>=0.5           # PDF → DOCX conversion (route 3)
mammoth>=1.7            # DOCX → HTML for in-browser preview
lxml>=5.0               # HTML DOM walking (route 2)
